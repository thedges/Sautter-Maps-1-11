@isTest
public class SautterMapsTest {
 static testMethod void testSautterMaps() {
    SautterMaps gm = new SautterMaps();
    SelectOption[] so = gm.MapLayers;
    gm = new SautterMaps(null); 
  }     
}